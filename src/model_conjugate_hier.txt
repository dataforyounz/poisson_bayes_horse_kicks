model{

  for( j in 1:n_corp ){
    for( i in 1:n_obs ){
      death_mat[i,j] ~ dpois( lambda[j] )
    }
    
    lambda[j] ~ dgamma( alpha, beta )
    
  }
  
  alpha ~ dgamma(1, .1)
  beta ~ dgamma(1, .1)
}